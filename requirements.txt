# Core API (Railway only)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-dotenv
requests

# Auth & Database
supabase>=2.3.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Rate limiting
slowapi>=0.1.9

# LiveKit API only (no agent/plugins)
livekit>=0.12.0